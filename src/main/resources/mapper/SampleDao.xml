<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="sampleDao"> <!-- Mapper의 FQCN을 namespace으로 한다. -->



		<select id="selectSample001" resultType="test.com.study.sample.vo.SampleVo">
		
			SELECT 
			    ID AS id
			   ,PASSWORD AS password
			FROM test.sample
			LIMIT 1
			
		</select>











</mapper>